# よりそいPHR（線維筋痛症版）初版 要件定義 ― 痛み×交流会ループ

## 0. この文書の使い方（Cursor向け）

- 対象は**初版のみ**。薬管理系（写真で薬登録／今日の薬／服薬確認LINE／検査値写真）は第2弾で、この文書の範囲外。
- 既存リポジトリ `yorisoi_phr`（React + TypeScript）を土台に実装する。
- 医療・患者データを扱うため、匿名性とアクセシビリティは非機能ではなく**機能要件**として扱う（第6・7章）。
- 外部API（LINE Messaging API、気圧API、`@google/genai` 等）のシグネチャは変動が速い。実装時に各公式ドキュメントで最新仕様を確認してから固定すること。

---

## 1. 初版の設計目標

**「痛みを記録 → グラフ化 → 交流会で持ち寄る」ループを成立させる。**

患者会理事との打ち合わせで検証された中心価値は、個人の記録そのものより「交流会でみんなが持ち寄って比べ、話題のきっかけにする」こと。したがって初版は、出力（グラフ）を交流会で共有できる形にすることを最優先の設計目標とする。診察での提示は副次的用途として同じ出力を流用する。

---

## 2. スコープ

### 初版（この文書）
- 痛み記録（全体5段階 ／ 部位別3段階・人型フルタップUI）
- 部位別棒グラフ（期間集計・全体100%）
- 気分記録
- 気圧・気温の表示と値の保存
- 出来事メモ（自由記述＋固定タグ、音声入力補助）
- 痛みグラフ（気圧重ね・固定テンプレート・匿名）
- 出力とLINE配信（グラフ画像＋印刷用PDFリンク）
- 横断：タップ中心・大タップ領域・1日1タップ最小フロー

### 第2弾（範囲外）
- ①写真で薬登録（Gemini）／②今日飲むべき薬／③服薬確認LINE／④検査値写真
- ※ ①は別途Cursor指示書を作成済み。初版ローンチ後に載せる。

---

## 3. 技術スタックと前提

| 層 | 採用 |
|---|---|
| フロント | 既存 `yorisoi_phr`（React + TypeScript） |
| 認証 | LINE Login / LIFF（push配信に必要なLINE userIdを取得するため） |
| バックエンド | Node.js + TypeScript（Cloud Run） |
| グラフ/PDF生成 | HTMLテンプレート → ヘッドレスChromium（Puppeteer等）でPNG＋A4縦PDFを出力 |
| 生成物ホスティング | Cloud Storage（HTTPSのURLで配信。LINE画像・PDFリンク用） |
| 通知配信 | LINE Messaging API（画像メッセージ＋リンク） |
| 気圧・気温 | 気圧APIをユーザー地域の緯度経度で取得し保存 |
| DB | PostgreSQL（Prisma） |

---

## 4. 全体構成 / データフロー

```
[React (yorisoi_phr / LIFF)]
  ・毎日の記録：全体痛み5段階 / 人型で部位別3段階 / 気分 / メモ・タグ
  ・当日の気圧・気温を表示（取得値は記録に同梱保存）
  ・「グラフを作る」操作
        │
        ▼
[Cloud Run: report API (Node/TS)]
  ・期間の記録を集計
  ・固定テンプレHTMLをレンダリング → PNG（LINE用）＋ A4縦PDF（印刷用）
  ・Cloud Storageに保存しHTTPS URLを取得
  ・LINE Messaging APIで配信
        │
        ▼
[LINE]
  ・画像メッセージでグラフを表示（プレビュー）
  ・テキスト/Flexで「印刷用PDF」リンクを併記
```

---

## 5. 機能要件

### 5.1 痛み記録（二層尺度）

痛みは二層で記録する。患者の「日付の振り返りは5段階」「部位は丸の濃さで3段階」という声に対応する。

**(A) 全体の痛み：5段階（1日1タップ）**
- その日の全身の痛みを1〜5でタップ記録。
- これがグラフ（5.6）の縦軸になる主指標。
- ホームから1タップで記録完了できること（5.6・第6章の最小フローの中核）。

**(B) 部位別の痛み：人型フルタップUI・3段階**
- 前面・背面の人体シルエットを表示し、部位をタップして痛みを記録。
- 左右を区別する（左右差の記録が患者要望）。
- 各部位は3段階（丸の濃さで表現：淡＝軽 / 中 / 濃＝強）。
- 対象部位（左右がある部位は左右で別レコード）：
  頭 / 顔 / 首 / 肩 / 上腕 / 肘 / 前腕 / 手 / 胸 / 腹 / 背中上部 / 腰 / 臀部 / 大腿 / 膝 / 下腿 / 足
- (B)は任意入力。記録しない日があってよい（(A)だけで最小フローが成立する）。

### 5.2 部位別棒グラフ（期間集計・全体100%）
- 選択期間内の部位別記録を集計し、全体を100%として各部位の割合を棒グラフ表示。
- 既定は**記録回数ベース**（「どれくらいタップしているか」に忠実）。強度加重は将来オプション。
- 左右を分けて表示できること。

### 5.3 気分記録
- その日の気分を5段階でタップ記録（1タップ）。
- 交流会PDFには既定で出さない（プライベート寄り）。診察・本人閲覧用。

### 5.4 気圧・気温（表示＋値保存）
- ホームで当日の気圧（hPa）・気温（℃）を表示。
- **表示した値をその日の記録に同梱して保存する**（グラフの気圧重ねと、将来の「気圧×痛み」相関のため。値を捨てない）。
- 取得はユーザー登録地域の緯度経度で行う。

### 5.5 出来事メモ（自由記述＋固定タグ）
- 「この日なにかあった」を自由記述で残せる。
- 固定タグを併設：天候 / 活動しすぎ / 睡眠不足 / ストレス / 月経（他は運用で調整）。
- **自由記述は音声入力を補助として用意**（手が使えない日への配慮、第6章）。
- メモ・タグは既定で交流会PDFに出さない（本人・診察用）。

### 5.6 痛みグラフ（気圧重ね・固定テンプレート・匿名）
- 横軸＝日付、縦軸＝全体の痛み5段階の折れ線。
- 気圧を同じ時間軸に重ねる（第2軸または背景の帯）。
- 出来事タグがある日は点注釈（本人・診察用出力のみ。交流会用には出さない）。
- **固定テンプレート**：全ユーザーで縦軸・期間・体裁を統一し、持ち寄って比較できるようにする（第8章）。
- 期間は1ヶ月／3ヶ月の切替（既定は交流会サイクルに合わせる。第13章で理事確認）。

### 5.7 出力とLINE配信

**出力物（1枚のレポート）に載せる要素**
- 痛み推移の折れ線（気圧重ね）
- 部位別棒グラフ（全体100%）
- 人型ヒートマップ（期間内でよく痛んだ部位を色で集計表示）
- 表示名は匿名（ニックネームのみ）。生の日次ログは載せない。

**生成と配信**
- report APIが固定テンプHTMLをレンダリングし、**PNG（LINE画像用）＋ A4縦PDF（印刷用）**を出力してCloud Storageに保存。
- LINE配信は次の2メッセージ：
  1. **画像メッセージ**：グラフPNGを表示（`originalContentUrl` と `previewImageUrl` にHTTPSのURLを指定。プレビューが効く）。
  2. **テキスト or Flexメッセージ**：印刷用PDFのダウンロードリンクを併記。
- ※ LINEのbot送信（push）にPDFなどファイルを直接添付するメッセージ種別は存在しないため、PDFは必ずHTTPSリンクとして渡す。
- 生成トリガーは初版ではオンデマンド（アプリ内「グラフを作る」）を必須とし、交流会サイクルに合わせた自動配信は任意で追加。

**LINE画像メッセージの例**
```json
{
  "type": "image",
  "originalContentUrl": "https://storage.example.com/reports/xxxx.png",
  "previewImageUrl": "https://storage.example.com/reports/xxxx_preview.png"
}
```

---

## 6. 横断要件：アクセシビリティ（機能要件）

「ペンが持てず紙に書き込めない」ことが本アプリの存在理由。全機能に以下を効かせる。

- **全入力をタップで完結**（痛み5段階・部位別・気分・タグ）。大きいタップ領域（最小44〜48px相当）。
- **自由記述メモのみ音声入力を補助**として提供。
- **1日1タップ最小フロー**：調子が悪い日ほど記録が要るのに操作しづらい、という矛盾を前提に、ホームから「今日の全体痛み5段階」だけは1タップで残せる導線を用意する。部位別・メモ等は任意で、無理なく足せる設計にする。

---

## 7. 匿名性・プライバシー（論点H：匿名性は高く）

- 交流会で回覧される出力は共有物である前提で設計する。
- 出力の表示名は**ニックネーム/匿名のみ**。実名・生年月日・連絡先を載せない。
- 交流会用出力には**グラフのみ**を載せ、日次の生ログ・自由メモ・タグ・気分は出さない。
- 個人メモ・タグ・気分は本人閲覧および（本人が選べば）診察用出力にのみ表示。
- 出力に載せる項目はユーザーが確認できること。

---

## 8. 出力様式の標準化（論点I）

- 交流会で持ち寄って比較するのが目的のため、**全ユーザーで縦軸・期間・レイアウトを統一**する。
- ユーザーごとに軸や期間が変わる自由度は初版では持たせない（比較可能性を優先）。
- テンプレートは固定。差し替えは全体一括で行う。

---

## 9. データモデル（Prisma・概略）

```prisma
model User {
  id        String  @id @default(cuid())
  lineUserId String? @unique     // LINE push配信用
  nickname  String                // 匿名表示名
  areaLat   Float?                // 気圧取得用
  areaLng   Float?
  records   DailyRecord[]
}

model DailyRecord {
  id           String   @id @default(cuid())
  userId       String
  date         DateTime                 // 記録日（1日1件）
  overallPain  Int                       // 全体の痛み 1..5
  mood         Int?                      // 気分 1..5
  pressureHpa  Float?                    // 当日気圧（表示値を保存）
  temperatureC Float?                    // 当日気温
  note         String?                   // 自由メモ
  tags         String[]                  // 固定タグ
  bodyPains    BodyPain[]
  createdAt    DateTime @default(now())
  @@unique([userId, date])
}

model BodyPain {
  id        String   @id @default(cuid())
  recordId  String
  region    BodyRegion
  side      Side                        // left | right | center
  intensity Int                          // 部位別 1..3（丸の濃さ）
}

enum Side { left right center }

enum BodyRegion {
  head face neck shoulder upperArm elbow forearm hand
  chest abdomen upperBack lowerBack buttock thigh knee lowerLeg foot
}
```

---

## 10. バックエンド（Cloud Run）

- **report API**：期間の記録を集計 → 固定テンプHTMLをレンダリング → PNG＋A4縦PDF出力 → Cloud Storage保存 → HTTPS URL取得 → LINE配信。
- **気圧取得**：ユーザー地域の緯度経度で気圧API取得。当日値を `DailyRecord` に保存。
- **環境変数/シークレット**：LINEチャネルアクセストークン、気圧APIキー、Cloud Storageバケット、`ALLOWED_ORIGIN`。いずれもSecret Manager経由でマウントし、フロントに露出させない。
- PNGは元画像とプレビュー（小サイズ）の2枚を出力する（LINE画像メッセージ仕様）。
- HTTPSはTLS必須。生成物URLは推測困難にする（署名付きURL等）。

---

## 11. 受け入れ条件（Definition of Done）

- [ ] 全体の痛みを5段階でホームから1タップ記録できる。
- [ ] 人型（前後＋左右）で部位別の痛みを3段階（丸の濃さ）で記録できる。
- [ ] 期間内の部位別割合が全体100%の棒グラフで表示される（左右区別）。
- [ ] 当日の気圧・気温が表示され、その値が記録に保存される。
- [ ] 出来事メモ（自由＋固定タグ）が記録でき、自由記述は音声入力できる。
- [ ] 痛み推移グラフに気圧が重なって表示される。
- [ ] 出力（1枚）が固定テンプレートで生成され、表示名は匿名、生ログは載っていない。
- [ ] 出力がPNG（LINE画像）＋A4縦PDF（印刷）で生成され、Cloud StorageのHTTPS URLになる。
- [ ] LINEに画像メッセージ＋PDFリンクが届く。
- [ ] 全入力がタップで完結し、タップ領域が十分大きい。
- [ ] LINEトークン・APIキーがフロントのバンドルに含まれない。

---

## 12. 実装順序（Cursorタスク分解）

1. データモデル（第9章）とPrismaマイグレーション。
2. LINE Login/LIFFでユーザーとlineUserIdを紐付け。
3. 記録UI：全体痛み5段階（1タップ最小フロー）→ 気分 → 気圧表示・保存。
4. 人型フルタップUI（前後シルエット＋左右、3段階）と部位別レコード保存。
5. 部位別棒グラフ（期間集計・全体100%）。
6. 出来事メモ（自由＋固定タグ、音声入力補助）。
7. 気圧API連携（取得・保存）。
8. report API（Cloud Run）：集計 → 固定テンプHTML → PNG＋PDF出力 → Cloud Storage保存。
9. LINE配信（画像メッセージ＋PDFリンク）。
10. 匿名性・出力様式の固定（第7・8章）を出力に反映。
11. 受け入れ条件（第11章）を順に確認。

---

## 13. 理事に確認する保留事項

- **グラフ期間の既定**：交流会の開催サイクル（月次か、数ヶ月ごとか）。それに合わせて既定期間を決める。確認前は1ヶ月/3ヶ月切替で仮置き。
- **匿名表示名の既定**：ニックネーム必須か、完全匿名ラベル（例：参加者A）まで許すか。
- **交流会PDFに載せてよい範囲**：痛み推移＋部位別＋人型ヒートマップまで、で問題ないか。

---

## 付記：実装時に必ず確認すること
- LINE Messaging APIの画像メッセージ仕様（URL・サイズ上限・TLS要件）と、push配信でファイル添付種別が無いこと。
- ヘッドレスChromiumのCloud Run上での動作（依存ライブラリ・メモリ）。
- 気圧APIの提供元・取得頻度・利用規約。
